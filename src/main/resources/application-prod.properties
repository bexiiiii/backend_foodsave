# Database Configuration
spring.datasource.url=${DATABASE_URL:jdbc:postgresql://31.130.152.106:5432/foodsave}
spring.datasource.username=${DATABASE_USERNAME:behruz}
spring.datasource.password=${DATABASE_PASSWORD:234Bex456}
spring.datasource.driver-class-name=org.postgresql.Driver

# JPA/Hibernate Configuration
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.properties.hibernate.format_sql=false
spring.jpa.generate-ddl=true

# Connection Pool Settings
spring.datasource.hikari.connection-timeout=60000
spring.datasource.hikari.maximum-pool-size=30
spring.datasource.hikari.minimum-idle=10

# Server Configuration - ТОЛЬКО localhost (через nginx)
server.address=127.0.0.1
server.port=8080
app.base-url=https://admin.foodsave.kz/api

# JWT Configuration
security.jwt.secret=foodsaveSecretKeyProduction123456789_super_long_64_byte_minimum_value_OK_example_ABC123

app.jwt.expiration=86400000

# CORS Configuration
cors.allowed-origins=https://foodsave.kz,https://miniapp.foodsave.kz,https://admin.foodsave.kz,https://partner.foodsave.kz,https://vendor.foodsave.kz,https://web.telegram.org,https://miniapp.telegram.org,https://t.me
telegram.support.username=@FoodSave_bot

# Swagger/OpenAPI - ОТКЛЮЧЕНО в продакшене для безопасности
springdoc.api-docs.enabled=false
springdoc.swagger-ui.enabled=false

# Tomcat настройки для высокой нагрузки и защиты от DDoS
server.tomcat.threads.max=200
server.tomcat.threads.min-spare=20
server.tomcat.max-connections=10000
server.tomcat.accept-count=100
server.tomcat.connection-timeout=20000
# Отклонять слишком большие заголовки (защита от атак)
server.max-http-request-header-size=8KB

# Logging Configuration
logging.level.com.foodsave=INFO
logging.level.org.springframework.security=WARN
logging.level.org.hibernate.SQL=WARN
logging.level.org.springframework.web=WARN
logging.level.com.zaxxer.hikari=INFO
# Скрываем ошибки парсинга HTTP (боты/сканеры)
logging.level.org.apache.coyote.http11=ERROR

logging.level.org.hibernate=INFO
logging.level.io.lettuce.core=INFO

logging.level.root=INFO
logging.level.org.springframework=INFO

# Client bot token (for regular users with Mini App)
telegram.bot.token=${TELEGRAM_BOT_TOKEN:7773680612:AAEusECkUXmf7mTpw1JmMLWE5KQGJ1spYMc}

# Manager bot token (for store managers)
telegram.manager.bot.token=${TELEGRAM_MANAGER_BOT_TOKEN:7773680612:AAEusECkUXmf7mTpw1JmMLWE5KQGJ1spYMc}

# Redis Cache Configuration (fallback to simple if Redis unavailable)
spring.cache.type=${CACHE_TYPE:redis}
spring.data.redis.host=${REDIS_HOST:31.130.152.106}
spring.data.redis.port=${REDIS_PORT:6379}
spring.data.redis.password=${REDIS_PASSWORD:}
spring.data.redis.ssl.enabled=${REDIS_SSL:false}
spring.data.redis.repositories.enabled=false
spring.data.redis.timeout=2000
spring.cache.redis.time-to-live=600000
